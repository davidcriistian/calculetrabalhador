# Canonical domain package standard

Each domain is a self-contained knowledge package. Identity, sources, concepts, rules, exceptions, temporal policy, projections, dependencies, tests and governance remain separated.

A package may advance only through maturity gates. `consumerReady` remains false until all gates pass. Consumers and automations may read published canonical records but may never author legal knowledge directly.
