{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/data/manifests/schemas/dependency-map.schema.json",
  "title": "Manifest Dependency Map",
  "type": "object",
  "required": ["id", "version", "status", "runtimeStatus", "createdAt", "updatedAt", "owner", "source", "dependencies"],
  "properties": {
    "id": { "type": "string" },
    "version": { "type": "string" },
    "schema": { "type": "string" },
    "status": { "type": "string" },
    "runtimeStatus": { "type": "string" },
    "createdAt": { "type": "string" },
    "updatedAt": { "type": "string" },
    "owner": { "type": "string" },
    "source": { "type": "string" },
    "description": { "type": "string" },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["operationId", "requires"],
        "properties": {
          "operationId": { "type": "string" },
          "requires": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "dependencyPrinciples": { "type": "array", "items": { "type": "string" } }
  }
}
